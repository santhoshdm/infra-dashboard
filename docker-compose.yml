services:
  backend:
    build: ./backend
    environment:
      - AWS_DEFAULT_REGION=ap-south-1
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always